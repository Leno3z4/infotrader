[project]
name = "infotrader-cloudflare"
version = "0.1.0"
description = "Cloudflare Worker entrypoint for InfoTrader"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
